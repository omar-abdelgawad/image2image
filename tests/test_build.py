"""Test module for making sure model package is working"""

from img2img.models import pix2pix, tunit, unit


def test_build():
    """Make sure pytest is working."""
    assert True
    assert pix2pix
    assert tunit
    assert unit
