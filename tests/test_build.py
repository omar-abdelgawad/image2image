"""Test module for making sure model package is working"""
import pix2pix
import tunit
import unit


def test_build():
    """Make sure pytest is working."""
    assert True
    assert pix2pix
    assert tunit
    assert unit
